# v1.12.5 — «Кофе с собой» это общепит

Прогон по 11 подрубрикам закрыл реестр на 2707 организаций. Внутри ЮЗАО
отбраковано шесть строк, из них пять — правильно (Лэтуаль, торговый центр,
магазин нижнего белья ×2, хлебозавод). Шестая — нет:

```
Кофе твой друг   |  Кофе с собой
```

«Кофе с собой» — самостоятельная рубрика Яндекса, и в списке токенов группы
«общепит» её не было: там есть «кофейн», но точка с единственной рубрикой
«Кофе с собой» под неё не подходит. Нашёл её новый запрос «магазины чая кофе»
(категория розницы), совпадения по категории нет, группы нет — отбраковка.

Просто добавить токен нельзя: «Кофе с собой» стоит в рубриках у 259 организаций
реестра, и почти все они — Пятёрочки и Перекрёстки с кофейным автоматом. Они
магазины, а не кофейни, и приписывать их к общепиту значит испортить разбивку
по группам.

Поэтому правило с оговоркой: «кофе с собой» относит организацию к общепиту
только если в рубриках нет признаков магазина (`магазин`, `маркет`, `лавка`,
`супермаркет`, `минимаркет`, `торгов`). Точка с одной рубрикой «Кофе с собой» —
общепит; «Супермаркет, кофе с собой, магазин продуктов» — розница, как и было.
